---
import Base from '../../layouts/Base.astro';
import { getCollection } from 'astro:content';
import { UI, fmtDate } from '../../config.js';

const lang = 'zh';
const t = UI[lang];
const posts = (await getCollection('blog', ({ id, data }) => id.startsWith('zh/') && !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---
<Base title={`${t.brandMark} · ${t.brandName}`} description={t.heroDesc} lang={lang} path="/zh/">
  <section class="hero">
    <div class="wrap">
      <p class="eyebrow">{t.heroEyebrow}</p>
      <h1>{t.heroTitle}</h1>
      <p>{t.heroDesc}</p>
    </div>
  </section>

  <section class="post-list">
    <div class="wrap">
      <p class="list-label">{t.latest}</p>
      {posts.map((post) => (
        <a href={`/zh/blog/${post.slug.replace('zh/', '')}/`} class="post-item">
          <p class="post-meta">{fmtDate(post.data.pubDate, lang)}</p>
          <h2 class="post-title">{post.data.title}</h2>
          <p class="post-desc">{post.data.description}</p>
        </a>
      ))}
    </div>
  </section>
</Base>
